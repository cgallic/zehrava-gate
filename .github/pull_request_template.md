## What / why

<!-- One or two sentences: what changes, and what problem it solves. -->

## How tested

<!-- Which test files you ran/added, e.g.:
`npm test` in packages/gate-server (all 11 files), plus new
`test/slack-provider.test.js`. -->

## Checklist

- [ ] `npm test` passes in `packages/gate-server` (and any other touched package)
- [ ] `CHANGELOG.md` updated (Unreleased section) for user-facing changes
- [ ] Docs updated (README / policy examples) if behavior or interface changed
