[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "zehrava_gate"
version = "0.1.0"
description = "Zehrava Gate SDK — safe commit layer for AI agents"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
keywords = ["ai", "agents", "approval", "governance"]
dependencies = []
